---
import { projects } from '../../data/projects';
import { Github } from '@lucide/astro';
import { ExternalLink } from '@lucide/astro';
---

<section id="projects" class="py-16 scroll-mt-20 bg-white dark:bg-black">
  <div class="max-w-5xl mx-auto px-4">
    <h2 class="text-2xl md:text-3xl font-bold mb-8">Projects</h2>

    <div class="grid md:grid-cols-2 lg:grid-cols-3 gap-6">
      {projects.map(project => (
        <article class="flex flex-col p-5 bg-neutral-50 dark:bg-neutral-950 rounded-xl border border-neutral-200 dark:border-neutral-600 hover:border-primary-500 dark:hover:border-primary-500 transition-colors">
          <h3 class="font-semibold text-lg text-neutral-900 dark:text-neutral-1000 mb-2">
            {project.name}
          </h3>
          <p class="text-sm text-neutral-500 dark:text-neutral-400 mb-4 grow">
            {project.description}
          </p>

          <ul class="flex flex-wrap gap-2 mb-4">
            {project.technologies.slice(0, 4).map(tech => (
              <li class="px-2 py-0.5 text-xs bg-neutral-200 dark:bg-neutral-600 text-neutral-500 dark:text-neutral-400 rounded">
                {tech}
              </li>
            ))}
          </ul>

          <div class="flex items-center gap-3 pt-3 border-t border-neutral-200 dark:border-neutral-600">
            {project.github && (
              <a
                href={project.github}
                target="_blank"
                rel="noopener noreferrer"
                class="inline-flex items-center gap-1.5 text-sm text-neutral-500 hover:text-neutral-900 dark:text-neutral-400 dark:hover:text-neutral-1000 transition-colors"
              >
                <Github class="w-4 h-4" />
                Code
              </a>
            )}
            {project.demo && (
              <a
                href={project.demo}
                target="_blank"
                rel="noopener noreferrer"
                class="inline-flex items-center gap-1.5 text-sm text-neutral-500 hover:text-neutral-900 dark:text-neutral-400 dark:hover:text-neutral-1000 transition-colors"
              >
                <ExternalLink class="w-4 h-4" />
                Demo
              </a>
            )}
          </div>
        </article>
      ))}
    </div>
  </div>
</section>
